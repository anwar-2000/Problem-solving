function minSubArrayLen(arr :number[],target:number): number   {
    /**
     * 
     * inputs : array to itertate its values , target of the sum
     * 
     * 
     * output : 0 if there is no contiguous subarrays that are = > target , else return the length of it
     * 
     *   
     */


    //i'll work at it tommorow 
    return 1;
    
}